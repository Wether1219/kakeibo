# CLAUDE.md

このファイルはClaude Codeがセッション開始時に自動で読み込むプロジェクト共通コンテキストです。
**ここに書いてある内容は毎回のプロンプトで説明し直さないこと。** 詳細が必要な場合のみ `docs/` 内の該当ファイルを読むこと。

## 言語設定
- 常に日本語で会話・応答する
- コードコメントも日本語で記述する
- エラーメッセージの説明も日本語で行う

## プロジェクト概要
2人世帯向け家計簿Webアプリ（既存Excelマクロブックのリプレイス）。
詳細要件は `docs/01_要件定義書.md` 参照（読むのは実装対象機能の該当節のみでよい）。

## 技術スタック（確定事項・変更しない）
- フロントエンド: React + Vite + TypeScript + Tailwind CSS + Chart.js
- バックエンド: Node.js + Express + TypeScript + Prisma ORM
- DB: MySQL 8.x（Docker Compose、`docker-compose.yml`参照）
- 認証: JWT + bcrypt
- 文字コード: utf8mb4（絵文字アイコン費目のため必須）

## ディレクトリ構成
`docs/04_開発計画書.md` 2章のフォルダ構成に従う。新規ファイルもこの構成を踏襲すること。

## コーディング規約
- テーブル/カラム名: スネークケース。API/TS: キャメルケース
- コミットメッセージ: `feat: 〜 (F03)` `fix: 〜` `docs: 〜` 形式、機能IDを付与
- 1機能=1ブランチ（`feature/f03-transaction-input`のように機能IDを含める）
- 集計ロジック（按分・週次判定・比率計算）は必ず単体テストを書く（`docs/03_詳細設計書.md` 5章のロジック定義に厳密に従う）
- 全APIで `household_id` によるデータ分離を必須とする（他世帯データへのアクセス禁止）

## ドキュメント参照ルール（トークン節約のため厳守）
- テーブル定義が必要な時 → `docs/03_詳細設計書.md` の該当テーブルの節のみ読む（1章全部は読まない）
- API仕様が必要な時 → 同ファイル4章の該当行のみ読む
- 画面仕様が必要な時 → 同ファイル3章の該当画面のみ読む
- 既に読んだ内容は再度読み直さず、自分の実装計画メモとして要約を残す

## 実装フロー（機能ごとに必ずこの順で進める）
1. 対象機能の要件・設計該当箇所を読む（該当節のみ）
2. 実装計画を箇条書きで提示し、着手前に承認を得る（大きな出力は作らない）
3. DBマイグレーション → API → フロントの順に実装
4. 単体テスト作成・実行
5. 完了したらこのCLAUDE.mdの「進捗」セクションを更新する

## 進捗（Claude Codeが実装完了ごとに追記すること）
- [x] フェーズ0: 環境構築・認証
  - [x] docker-compose.yml / backend・frontend初期構成（package.json, tsconfig, Prisma, Vite+Tailwind）
  - [x] JWT認証（F01/SC01）実装済み（2026-08-15）。docs/03_詳細設計書.md 1.2節・4章に従い、`users`テーブルに`email`(UNIQUE)/`password_hash`/`color_code`を追加するマイグレーション（`20260815120000_add_user_auth`、既存行には開発用の仮アカウントを自動付与）。`bcrypt`でハッシュ化・照合、`jsonwebtoken`でJWT発行・検証（`backend/src/services/authService.ts`、`JWT_SECRET`は`backend/.env`）、`POST /auth/login`を実装（`backend/src/routes/auth.ts`）。暫定ヘッダー認証（`x-household-id`/`x-user-id`）は完全に廃止し、`backend/src/middlewares/household.ts`の`authMiddleware`が`Authorization: Bearer <JWT>`を検証してhouseholdId・userIdの両方を取得する方式に全11ルートファイルを置き換え（旧`userMiddleware`は不要になったため削除）。既存バックエンドテスト13ファイル＋新規`tests/auth.test.ts`（ログイン成功/失敗、トークン検証）で計113テスト全通過確認済み（`--no-file-parallelism`時。デフォルトの全ファイル並列実行時は`excelMigration.test.ts`・`summary.test.ts`の重い集計テストがマシン負荷次第でまれにタイムアウトすることがあるが、認証とは無関係の既存の性能特性でロジック自体は正しい。テスト間で`household_id`定数が重複していた既存バグ〔`999979n`・`999959n`が複数ファイルで衝突〕も本タスクで解消済み）。フロントはSC01ログイン画面（`frontend/src/pages/SC01_Login.tsx`、設計書に画面仕様の記載がないためシンプルなフォーム）・共通APIクライアント（`frontend/src/api/client.ts`の`apiFetch`、トークンは`localStorage`の`kakeibo_access_token`に保存し全リクエストへ`Authorization`ヘッダーを自動付与、401時は自動でログイン画面へ遷移）を新規実装し、既存の全APIクライアントファイル（`categories.ts`等9ファイル）から`TEMP_HOUSEHOLD_ID`・`ensureCurrentUserId`等の暫定コードを撤去。`App.tsx`に未ログイン時のリダイレクトガードを追加し、`NavBar.tsx`にログアウトボタンを追加。SC03（取引入力画面）の「あなたは：」ユーザー切替UI（暫定認証時代の名残）は、ログイン中のユーザーが自動的に「自分」になるため削除（`useTransactionForm.ts`・`TransactionInput.tsx`）。開発用DBの既存ユーザー（たいよう/みらの、household_id=1）には開発用の仮パスワード`kakeibo1234`を設定済み（email: `taiyo@example.com`/`mirano@example.com`）。`tsc --noEmit`・`vite build`通過確認済み、`curl`でログイン→トークン取得→認証必須APIアクセスまで実動作確認済み（ブラウザでのインタラクティブな実動作確認は本セッションでは未実施）
- [ ] フェーズ1: コア機能（取引入力/収入・先取り貯金入力/ダッシュボード/費目マスタ）
  - [x] 費目マスタ（categoriesテーブル、`/categories` API、SC10画面）実装済み（2026-08-13）
  - [x] 取引入力（transactionsテーブル、`/transactions` CRUD API）実装済み（2026-08-13）。SC03画面（フロント）も実装済み（2026-08-14、`frontend/src/pages/TransactionInput.tsx`）。設計書3.1の2段階費目選択（固定費/変動費→費目）を維持しつつ、①区分→②費目→③対象者→④保存の4タップで完了する構成（金額はテンキー入力のためタップ数に含めない）。「自分」が誰かは認証未実装のため`localStorage`（`kakeibo_current_user_id`）で暫定管理し、対象者トグルの初期値・`x-user-id`ヘッダーに利用（`frontend/src/hooks/useTransactionForm.ts`、JWT認証実装時に置き換えること）。保存後は日付・対象者・金額・メモをリセットし連続入力に対応（区分・費目は直前の選択を維持し再入力を高速化）。Playwrightでdev環境（backend:3001 / frontend:5173）に対し実際にフォーム入力→保存→DB反映まで動作確認済み（コンソールエラーなし）
  - [x] 収入・先取り貯金入力（incomes/pre_savingsテーブル、`/incomes`・`/pre-savings` の一覧取得＋一括更新API、SC05画面）実装済み（2026-08-13）。カテゴリのtype整合性チェック（income/pre_saving）・household分離を含む。SC05表示のため`users`テーブルに`display_name`・`household_id`を先行追加し、`GET /users`（household内一覧）も追加。フロントは`App.tsx`に簡易タブナビ（react-router未導入、暫定）で費目マスタ画面と切替表示
  - [x] ダッシュボード：`GET /summary/monthly` API実装済み（2026-08-13）。`backend/src/services/summaryLogic.ts`に5.1按分（shared端数はcreatedBy側に+1円）・5.2週番号・5.3当月収支サマリ・5.4比率計算（ゼロ除算ガード）を純粋関数として実装し単体テスト済み（`tests/summaryLogic.test.ts`）。`summaryService.ts`でDB集計しAPI化、統合テストは`tests/summary.test.ts`。2026-08-14、SC02使用API対応のため`fixedExpenseByCategory`/`variableExpenseByCategory`の各要素を`{category, [displayName]: amount}`から`{categoryId, icon, name, amounts: {userId: amount}}`形式に変更（世帯人数が可変でもフロント側で対応できるよう）。あわせて`GET /transactions`に`limit`・`sort=date_desc`（それ以外の値は400）を追加（`transactionService.ts`の`listTransactions`に`take`反映）。SC02画面（フロント）実装済み：`frontend/src/pages/SC02_Dashboard.tsx`・`hooks/useDashboard.ts`。`components/YearSelector.tsx`・`MonthSelector.tsx`・`BalanceSummaryCard.tsx`・`ExpensePieCharts.tsx`・`WeeklyBudgetProgress.tsx`・`RecentTransactions.tsx`・`components/charts/`（DoughnutChart/LineChart/StackedBarChart、react-chartjs-2ラッパー）・`constants/categoryColors.ts`（固定費7色・変動費9色パレット）を実装し`App.tsx`の簡易ナビに「ダッシュボード」タブとして組み込み済み
- [ ] フェーズ2: 予算・集計（週次予算/年間推移/収支可視化）
  - [x] 週次予算（weekly_budgetsテーブル、`/weekly-budgets`の一覧取得〔実績付き〕＋一括更新API）実装済み（2026-08-13）。テーブルは予算額のみ保持し、実績は`summaryLogic.ts`の`apportionTransactionAmount`（5.1按分・世帯全ユーザー分を合算）と`calculateWeekNumber`（5.2週番号）を再利用してtransactionsから都度集計（`weeklyBudgetService.ts`）。カテゴリのtype整合性チェック（variable_expense）・household分離を含む。`calculateWeekNumber`は月末が日曜の場合に第6週を返しうるためweekNoは1〜6を許容（設計書1.7節「1〜5」はTINYINTコメントであり上限ではない）。GETは月末を含む週まで費目×週の全組み合わせを返す（予算・実績が0件の週も含む、SC06の週表示自動切替に対応）。単体テストは`tests/weeklyBudgets.test.ts`。SC06画面（フロント）は実装済み（`frontend/src/pages/WeeklyBudget.tsx`、commit `7906a55`）。※本行は2026-08-13時点で「SC06画面は未着手」と記載していたが、実際には別途実装済みでCLAUDE.mdへの記載が漏れていたため2026-08-15に訂正
  - [x] 年間推移（SC07、`GET /summary/annual?year=`）実装済み（2026-08-14）。`summaryService.ts`の`getAnnualSummary`で、`income`/`pre_saving`は各月のincome.amount／preSaving.actualAmountを費目×ユーザー単位で集計、`fixed_expense`/`variable_expense`は既存の`apportionTransactionAmount`で按分してtransactionDateの月に加算し、有効な費目×世帯全ユーザーの組み合わせごとに`months[0..11]`（1月=index0、データなし月は0円）と`annualTotal`を持つ行を返す（`docs/06_画面補足仕様書.md`のレスポンス形式に準拠、`categoryName`はicon+name連結）。単体テストは`tests/summary.test.ts`（12ヶ月全データあり／一部月データなしの0埋めケース）。SC07画面（フロント）実装済み（2026-08-14）：`frontend/src/pages/SC07_AnnualTrend.tsx`（年セレクタ・区分タブ・クロス集計テーブル・費目プルダウン＋人別チェックボックスで系列切替する折れ線グラフ）、`components/CategoryTypeTab.tsx`・`AnnualCrossTable.tsx`（費目列`sticky`固定＋`overflow-x-auto`で横スクロール）、`hooks/useAnnualSummary.ts`、`api/summary.ts`に`fetchAnnualSummary`を追加。`App.tsx`の簡易ナビに「年間推移」タブとして追加。Playwrightでdev環境（backend:3001 / frontend:5174）に対しタブ切替・グラフ表示まで実際に動作確認済み（コンソールエラーなし）。2026-08-14、`components/MonthlySavingsSummary.tsx`を追加し、月々の貯金合計（ユーザー別＋合計、マイナスは赤字）を表示。貯金合計(e)=先取り貯金(b)+余り貯金(d)は展開すると収入(a)−支出(c)と同値（`docs/03_詳細設計書.md`5.3節）のため、API追加なしで既存rows（income/fixed_expense/variable_expense）から単純加減算のみで算出。当初は常時表示だったが、その後「収入」等と同様タブ切替式に変更（`CategoryTypeTab`に`SummaryTabType = CategoryType | 'savings_total'`を追加し「貯金合計」タブを新設、選択時は`AnnualCrossTable`の代わりに`MonthlySavingsSummary`とグラフ用ユーザーチェックボックスのみ表示、費目プルダウンは非表示）。さらに「固定費」「変動費」「先取り貯金」タブの費目選択プルダウンに「合計（全費目）」の選択肢を追加し（`TOTAL_OPTION`センチネル、`SC07_AnnualTrend.tsx`）、選択中区分内の全費目をユーザーごとに合算した月次推移をグラフで確認できるようにした（クロス集計テーブル下部の合計行と同じ集計）。加えて「貯金合計」タブに貯蓄率（`MonthlySavingsSummary.tsx`、ユーザー別行の下に小さめのグレー文字で表示、合計行にも同様）を追加。貯蓄率=貯金合計÷収入×100（`docs/03_詳細設計書.md`5.4節と同じゼロ除算ガード、収入0の月は0%）。年間合計欄の貯蓄率は月別比率の平均ではなく年間貯金合計÷年間収入で算出
  - [x] 収支可視化（SC08、F11、`GET /summary/visualization?year=`）実装済み（2026-08-14、commit `7a850aa`）。`summaryService.ts`の`getVisualizationSummary`でKPI（月平均・年間合計・支出率・貯蓄率等）を集計、単体テストは`tests/summary.test.ts`の`GET /api/v1/summary/visualization`。SC08画面（フロント）実装済み：`frontend/src/pages/SC08_Visualization.tsx`（`AverageBalanceSection`/`AnnualBalanceSection`/`KpiCards`/`RatioTrendChart`）、`hooks/useVisualizationSummary.ts`。`App.tsx`の`/visualization`ルート・`NavBar`に「収支可視化」として導線あり。※本行は実装当時にCLAUDE.mdへの記載が漏れていたため2026-08-15に追記
- [ ] フェーズ3: 資産管理・Excel移行
  - [x] Excelデータ移行スクリプト（`backend/src/migration/`）実装済み（2026-08-13）。docs/03_詳細設計書.md 6章に従い、費目マスタ→incomes→pre_savings→transactions→weekly_budgetsの順でテーブルごとに1関数（`importCategories.ts`等）を実装。CLIは`npm run migrate:excel -- <xlsmパス> <household_id>`（`runImport.ts`）。費目名セルは正規表現でアイコン/名前を分離（`splitIconName`、`excelUtil.ts`）。各月シートの年・月はシート名ではなくB1/B3セルの値を使用。取引の日付未入力行はスキップ（テンプレートの空行のため）。固定費・変動費で費目名が重複した場合は固定費側を優先マッピング（重複は現状未発生）。資産管理（assets/asset_balances）はテーブル未設計のためスコープ外・フェーズ3着手時に別途対応
  - [x] 移行テスト（`backend/tests/excelMigration.test.ts`）：アップロード雛形（`backend/tests/fixtures/雛形_家計簿_第1_4版.xlsm`、費目名以外ほぼ空欄のテンプレート）を全12ヶ月移行し、件数（費目24・収入120・先取り貯金96・週次予算480）と、年間推移シートの収入・固定費・変動費セル値をDB集計値（月次シートのSUMIFS式と同じ集計条件をJSで再現）と全月・全費目で突合。テンプレートが空データのため実質0円同士の一致検証に留まるが、加えて27年1月シートの一部セルに実データを模した値をメモリ上で書き込み、移行・集計ロジックが非ゼロ値でも元Excelの計算式（固定費=本人分のみ、変動費=本人分+「両方」分/2）通りに動くことを検証済み
  - [x] CSV/Excelインポートのアプリ内対応（F14拡張、`POST /import/excel`）実装済み（2026-08-15）。設計書4章で定義済みだった`POST /import/excel`（multipartファイル）を実装。`runImport.ts`を`runImportWorkbook(wb, householdId)`（共通コア）・`runImport(path, householdId)`（CLI用、`XLSX.readFile`）・`runImportBuffer(buffer, householdId)`（API用、`XLSX.read({type:'buffer'})`）に分離し、CLI（`npm run migrate:excel`）の挙動は変更していない。`backend/src/routes/import.ts`は`multer`（メモリストレージ、20MB上限）でファイルを受け取り、`authMiddleware`より先に`upload.single('file')`を実行する順序にしている（先に認証で401を返すとクライアントのファイル送信中にサーバーがボディを読まずレスポンスしてしまいECONNRESETになるため）。単体テストは`backend/tests/importApi.test.ts`（401・400・実ファイルでの取込成功・household分離、4件）。**重要な制約**: 既存の`importTransactions`は取引を常に新規作成（upsertではない）ため、同じファイルを2回インポートすると取引が重複する。この挙動はCLI版から変更せず、重複防止ロジックの新規実装もスコープ外としたため、フロント側で明確な警告文＋`window.confirm`確認ダイアログを表示する対応に留めている。フロントはSC11（設定・履歴画面）に「インポート」タブを追加（`frontend/src/api/import.ts`、`frontend/src/pages/SC11_Settings.tsx`）。`frontend/src/api/client.ts`の`apiFetch`はFormDataボディの場合`Content-Type`を固定しないよう修正（ブラウザにmultipart境界を自動設定させるため）。`tsc --noEmit`・`vite build`通過、実ファイルでdev環境（household_id=1）への取込成功を確認済み（費目24件更新・収入120件・先取り貯金96件・週次予算480件、ブラウザでのクリック確認は未実施）
  - [x] 資産管理（assets/asset_balancesテーブル、`/assets`のCRUD＋`/asset-balances`の一覧取得〔人別小計・世帯合計付き〕＋一括更新API）実装済み（2026-08-14）。docs/03_詳細設計書.md 1.8/1.9節に厳密に従いPrismaスキーマ追加（`AssetGroup` enum、`Asset`・`AssetBalance`モデル、マイグレーション`20260814134838_add_assets`）。`backend/src/services/assetService.ts`（口座CRUD、`ownerUserId`がhousehold内ユーザーであることを検証）・`assetBalanceService.ts`（`bulkUpsertAssetBalances`はweeklyBudgetServiceと同じbefore/after対付きupsertパターン、`listAssetBalances`は指定年の資産×1〜12月残高一覧に加え名義人別小計・世帯合計を算出して返す）。`routes/assets.ts`・`routes/assetBalances.ts`を追加しapp.tsに`/api/v1/assets`・`/api/v1/asset-balances`として登録。単体テストは`tests/assets.test.ts`・`tests/assetBalances.test.ts`（household分離・人別小計計算・他世帯assetId指定の拒否を含む、13件全通過）。SC09画面（フロント）実装済み（2026-08-14）：`frontend/src/pages/SC09_AssetManagement.tsx`（YearSelector＋`AssetGroupTab`〔現金・預貯金/証券・株式/保険〕＋`AssetBalanceTable`＋資産推移積み上げ棒グラフ）。`AssetBalanceTable`はセルクリック→input表示→blur/Enterで確定→`useAssetBalances`フック（`frontend/src/hooks/useAssetBalances.ts`）内で口座×月ごとに500msデバウンスして`PUT /asset-balances/bulk`を呼ぶ実装（保存中セルはスピナー代わりに「保存中...」テキスト表示、保存成功後に一覧を再取得して人別小計・合計行の集計値を更新）。人別小計・合計行はAPIレスポンスの集計値をそのまま表示し入力不可。名称列は`sticky`固定＋テーブル全体`overflow-x-auto`でスマホ横スクロール対応。`AddAssetModal`（名称/名義/詳細/用途/積立額、名義は`GET /users`で取得した世帯内ユーザーから選択）を追加し口座追加に対応。`frontend/src/api/assets.ts`・`assetBalances.ts`を新規追加、`App.tsx`の簡易ナビに「資産管理」タブとして組み込み済み。バックエンドAPI（口座追加・残高一括更新・人別小計/合計集計）はcurlで動作確認済み、フロントは`tsc --noEmit`・`vite build`が通ることを確認済み（Playwright等ブラウザでの実動作確認は未実施）。2026-08-14、口座の削除機能を追加：`AssetBalanceTable`の各行に「削除」ボタンを追加し、`window.confirm`後に`frontend/src/api/assets.ts`の`deactivateAsset()`（`PUT /assets/:id`に`isActive:false`を送る、既存の更新APIを流用した論理削除）を呼び出し、成功後に残高一覧を再取得する実装（`SC09_AssetManagement.tsx`）。バックエンドは新規エンドポイント追加なし。curlで削除後に`GET /asset-balances`から対象口座が消えることを確認済み。2026-08-14、SC09に「純資産」タブを追加：`AssetGroupTab`に`AssetGroupTabValue`型（`AssetGroup | 'net_worth'`）を導入しタブを1つ追加、選択時は既存の`GET /asset-balances`レスポンスの`ownerSubtotals`/`total`（既に現金・預貯金/証券・株式/保険の全区分合算値のためAPI追加不要）を新規`NetWorthSummary`コンポーネントで表示（名義人別小計＋純資産合計、口座追加ボタンや残高編集セルは非表示）。資産推移の積み上げ棒グラフは全タブ共通でそのまま表示継続。`tsc --noEmit`・`vite build`通過確認済み（ブラウザでの実動作確認は未実施）。同日、資産推移グラフを`StackedBarChart`（積み上げ棒）から`LineChart`（折れ線）に変更。試行錯誤の末、最終的に「資産推移（ユーザー表示名）」×世帯人数分＋「資産推移（総合計）」の複数グラフに分割する形に確定（`summary.ownerSubtotals`から人数分の名義人別セクションを動的生成、＋全員分を集計した総合計セクション）。各グラフ内は費目区分別（現金・預貯金／証券・株式／保険）合計の3系列固定（純資産/資産合計の重複線は含めない）。名義人別グラフは`summary.assets`を`ownerUserId`で絞り込んで集計。`tsc --noEmit`・`vite build`通過確認済み
- [ ] フェーズ4: 変更履歴・エクスポート・仕上げ
  - [x] 変更履歴（audit_logsテーブル、`GET /audit-logs`）実装済み（2026-08-13）。`backend/src/services/auditLogService.ts`の`recordAuditLog`/`recordAuditLogs`（bulk用）で記録し、`diff_json`は`{ before, after }`形式（該当なしはundefined）。既存の各`serializeXxx`関数を再利用しBigInt/Decimalを安全にJSON化。household分離・`?targetTable=&limit=`（デフォルト50・上限200）に対応
  - [x] 既存のPUT/POST/DELETE系APIに監査ログ記録処理を追加（2026-08-13）：`categories`（POST/PUT/DELETE）、`transactions`（POST/PUT/DELETE）、`incomes`・`pre-savings`・`weekly-budgets`（PUT /bulk、upsert前の既存行をまとめて取得しitem毎にcreate/update判定）。`audit_logs.user_id`がNOT NULLのため、これまで操作者IDを受け取っていなかった`categories`の全操作・`incomes`/`pre-savings`/`weekly-budgets`のbulk・`transactions`のPUT/DELETEに`userMiddleware`（`x-user-id`必須）を追加（既存テストは`x-user-id`ヘッダー付与＋ユーザーfixture追加で対応済み）。単体テストは`backend/tests/auditLogs.test.ts`
  - [x] CSV/Excelエクスポート（F15、`GET /export?format=csv|xlsx`）実装済み（2026-08-14）。`backend/src/services/exportService.ts`でhousehold単位のデータを取得しcsv文字列/xlsxバッファを生成、`backend/src/routes/export.ts`でファイル返却（householdMiddlewareのみ、更新系ではないためuserMiddleware不要）。xlsxは既存依存の`xlsx`パッケージ（移行スクリプトと共用）でマルチシート（費目マスタ／取引／収入／先取り貯金／週次予算）を生成。csvはマルチシート非対応のため取引一覧のみ出力（費目名・対象者名を人が読める形に変換、UTF-8 BOM付与でExcel文字化け対策）。assetsは未実装のためスコープ外。単体テストは`backend/tests/export.test.ts`（他世帯データが出力されないことを含む）。あわせてSC03（取引入力画面）の3タップ要件を設計レビューし、設計書3.1の2段階費目選択のままでは3タップを超えると判明したため、4タップ構成に方針変更のうえフェーズ1でSC03画面を実装（詳細は上記フェーズ1の該当行を参照）
  - [x] 費目マスタ管理のバグ修正（2026-08-14）：無効化（論理削除、`is_active=false`）した費目と同じ区分・名前で再追加しようとすると`@@unique([householdId, type, name])`制約に阻まれ409エラーになっていた（MySQLは部分ユニークインデックス非対応のため無効化済み行が制約を占有し続けるのが原因）。`categoryService.ts`の`createCategory`を修正し、同名の無効化済み費目が存在する場合はそれを再有効化（`isActive: true`、アイコンは指定があれば更新、idは維持）する仕様に変更。監査ログはこの場合`update`として記録（`routes/categories.ts`）。`backend/tests/categories.test.ts`にテスト追加、既存72件含め全テスト通過確認済み
  - [x] 費目マスタ管理のバグ修正（2026-08-14）：「費目マスタ管理」を最初の画面として開く（取引入力画面を一度も使わない）と費目を追加できない不具合を修正。`categories`等の更新系API（POST/PUT/DELETE）が必須とする`x-user-id`ヘッダーは、これまで`localStorage`の`kakeibo_current_user_id`にのみ依存しており、このキーは取引入力画面（`useTransactionForm.ts`）で対象者を明示的に切り替えたときしか書き込まれていなかったため未設定になりうるのが原因（バックエンドの401をフロントが握りつぶし「費目の追加に失敗しました」という汎用エラーになっていた）。`frontend/src/api/users.ts`に`ensureCurrentUserId()`（未設定なら世帯の先頭ユーザーを取得しlocalStorageへ保存）を追加し、`categories.ts`・`incomes.ts`・`preSavings.ts`・`weeklyBudgets.ts`の更新系関数（create/update/deactivate、bulk系）から呼び出すよう修正（同じ実装パターンだったincomes/preSavings/weeklyBudgetsも同時に修正）。PlaywrightでlocalStorageを空の状態から「費目マスタ管理」タブに直接遷移し費目追加が成功することを確認済み（コンソールエラーなし）
  - [x] ナビゲーション導線・年月状態の画面間共有・モバイル表示対応（2026-08-15）：これまでApp.tsxはルーティングライブラリ未導入で`useState<Screen>`によるタブ切替のみだったため、`react-router-dom`を新規導入（`frontend/package.json`）。`frontend/src/main.tsx`で`BrowserRouter`を追加し、`App.tsx`を`Routes`/`Route`ベースに書き換え（`/dashboard`, `/transaction`, `/income`, `/weekly-budget`, `/category-master`, `/annual-trend`, `/visualization`, `/asset-management`、ルート`/`は`/dashboard`へリダイレクト）。ナビは`frontend/src/components/NavBar.tsx`として新規実装し、画面下部固定・`overflow-x-auto`で横スクロールするボトムナビ形式に変更（8画面全てへの導線を1箇所に集約、375px幅でも項目が見切れず横スクロールで到達可能）。年・月の選択状態はこれまで各画面の`useState`にローカルクローズされ画面遷移で「今日」にリセットされていたため、`frontend/src/hooks/useYearMonthParams.ts`（`useYearParam`/`useYearMonthParams`、`react-router-dom`の`useSearchParams`を使用）を新規実装し、URLクエリパラメータ（`?year=&month=`）で状態を保持する方式に変更。SC02（year+month）・SC07/SC08/SC09（yearのみ）の該当箇所を置き換え、`NavBar`のリンクは現在のクエリパラメータを引き継いで遷移するため、例えばSC07で年を変更後にSC09へ遷移しても選択年が保持される（Playwrightで`annual-trend`で前年ボタン押下→`asset-management`へ遷移してもURLの`?year=`が引き継がれることを確認済み）。あわせて実装済み全8画面をPlaywrightで375px幅（`viewport: 375x812`）でスクリーンショット・DOM幅計測し、横はみ出し（`scrollWidth > clientWidth`）がないことを確認済み（元々各テーブルは`overflow-x-auto`対応済みだったため、今回の変更範囲では横はみ出しの新規発生・既存不具合とも検出されず）。`tsc --noEmit`・`vite build`通過確認済み
  - [x] 取引一覧・編集画面（SC04）実装済み（2026-08-15）。バックエンドAPI（`GET/PUT/DELETE /transactions`）は既存のものを利用。`frontend/src/hooks/useTransactionList.ts`で対象月の取引・費目・ユーザーを取得し、`frontend/src/pages/SC04_TransactionList.tsx`で年月セレクタ（既存`useYearMonthParams`再利用）・費目プルダウン・キーワード検索付きの一覧を表示。各行から`frontend/src/components/EditTransactionModal.tsx`（区分・費目・対象者・金額・日付・メモを編集、`PUT /transactions/:id`を利用）・削除（`DELETE /transactions/:id`）が可能。`App.tsx`に`/transactions`ルート、`NavBar`に「取引一覧」導線を追加し、ダッシュボード（SC02）の「もっと見る」ボタン（`RecentTransactions`の`onShowMore`、これまで未配線だった）から年月を引き継いで遷移するよう配線。同日、検索・ページネーションをサーバー側に切り替え：`GET /transactions`に`keyword`（メモ・費目名のOR部分一致、`transactionService.ts`の`buildWhere`で共通化）・`offset`を追加し、既存の`Transaction[]`というレスポンス形式は変えず総件数を`X-Total-Count`ヘッダーで返す設計にした（ダッシュボード等の既存呼び出し元に影響を与えないため）。フロントは新規`fetchTransactionsPage()`（`{items,total}`を返す、既存の`fetchTransactions`はダッシュボード用に維持）で1ページ20件・キーワード入力300msデバウンスのページネーションに対応。バックエンドテストは`tests/transactions.test.ts`に追加（keyword・offset・X-Total-Count、計17件）。`tsc --noEmit`・`vite build`通過確認済み、`curl`・実サーバーでAPI疎通確認済み（ブラウザでの実動作確認は未実施）
  - [x] 設定・履歴画面（SC11）実装済み（2026-08-15）。バックエンドAPI変更なし（既存の`GET /users`・`GET /audit-logs`・`GET /export`を利用）。`frontend/src/pages/SC11_Settings.tsx`にタブ切替（世帯メンバー／変更履歴／エクスポート）で実装：世帯メンバータブは`GET /users`による一覧表示のみ（**メンバーの追加・編集API（`POST/PUT /users`）は設計書に定義がなく未実装**。JWT認証実装でusersテーブルにemail/password_hashが必須になったため、メンバー追加＝アカウント発行の意味合いが強く、新規API設計を要する別タスクとして切り出し）。変更履歴タブは`GET /audit-logs?limit=100`を日時・操作種別・対象テーブル（日本語ラベル変換）・操作者で一覧表示し、`diff_json`は`<details>`展開でJSON表示。エクスポートタブはCSV/Excelのダウンロードボタン（`frontend/src/api/export.ts`の`downloadExport`、`apiFetch`でBlob取得しAuthorizationヘッダー付きダウンロードを実現、単純な`<a href>`は認証ヘッダーを送れないため不採用）。`frontend/src/api/auditLogs.ts`を新規追加。`App.tsx`に`/settings`ルート、`NavBar`に「設定・履歴」導線を追加。`tsc --noEmit`・`vite build`通過確認済み、`curl`でAPI疎通確認済み（ブラウザでの実動作確認は未実施）
  - [x] 取引入力（SC03）の3タップ化（2026-08-15）。取引データ（`POST/PUT /transactions`）は`categoryId`のみを送信し区分（固定費/変動費）は費目から一意に決まるため、従来の①区分→②費目→③対象者→④保存という4タップ構成から①区分選択ステップを廃止し、固定費・変動費をまとめた1つの費目選択グリッド（`frontend/src/pages/TransactionInput.tsx`、変動費セクションを先に表示）に変更。①費目→②対象者→③保存の3タップで完了する（設計書3.1の「2段階費目選択」から意図的に変更、要件定義書6章の3タップ以内という非機能要件を優先）。`useTransactionForm.ts`から`expenseType`/`setExpenseType`/`categoriesForType`を廃止し`variableCategories`/`fixedCategories`を公開する形に変更。バックエンド変更なし
  - [x] 費目アイコンの選択式ピッカー化（SC10、2026-08-15）。設計書5.2節「将来的にはアイコン選択制も検討」に対応。`frontend/src/constants/categoryIcons.ts`（家計簿でよく使う絵文字を約40種類収録）・`frontend/src/components/IconPicker.tsx`（ボタンタップで候補グリッドをポップオーバー表示、一覧にない絵文字は自由入力欄でも設定可）を新規実装し、費目マスタの新規追加フォーム（`CategoryMaster.tsx`）・既存費目編集行（`CategoryTypeSection.tsx`）の手入力テキストボックスを置き換え。バックエンドAPI変更なし
  - [x] CSV/Excelインポートの実行結果に基づき世帯人数拡張（3人以上）を確認（2026-08-15、実装は見送り）。`backend/src/services/summaryLogic.ts`の`apportionTransactionAmount`が「両方（折半）」を`Math.floor(amount/2)`で常に2等分する実装になっており、`summaryService.ts`（7箇所）・`weeklyBudgetService.ts`（1箇所）から呼ばれるダッシュボード・週次予算・年間推移・収支可視化の全てで、3人以上の世帯では「両方」取引の合計が実際の金額より過大に計算される（例：1000円の按分を3人世帯で行うと合計1500円相当になる）。フロントも`useTransactionForm.ts`（SC03）の対象者選択が`self/other/shared`という2人前提の型で3人目以降を個別選択できない（SC04の`EditTransactionModal`は全ユーザーをボタン列挙する実装のため個別選択可、回避策にはなる）。CLAUDE.mdのプロジェクト概要が「2人世帯向け家計簿Webアプリ」と明記し、要件定義書でも3人以上対応は「将来的」な検討事項とされているため、按分ロジック・対象者選択UIの修正は行わず確認結果の記録に留めた。3人以上対応が必要になった際は`apportionTransactionAmount`のシグネチャ変更（世帯人数を受け取りN等分）と全呼び出し元・既存テストの見直し、対象者選択UIの再設計が必要
  - [x] PC実機幅での表示確認（2026-08-15）：SC02(ダッシュボード)・SC07(年間推移)・SC08(収支可視化)・SC09(資産管理)の4画面をPlaywrightで一般的なノートPC〜デスクトップ幅（1280/1366/1440/1920px）で表示し、`document.documentElement.scrollWidth`がページ全体としてclientWidthを超える「横はみ出し」は発生していないことを確認。ただしSC09の資産残高テーブル（`AssetBalanceTable`、12ヶ月分＋名称列で必要幅1482px）は、ページ全体を`max-w-4xl`（896px）で中央寄せしていたため、画面に十分な余白があっても`overflow-x-auto`のテーブル内スクロールが常に必要になっていた（1440px幅で必要幅1482pxに対しテーブル領域はわずか862px）。`frontend/src/pages/SC09_AssetManagement.tsx`のラッパーを`max-w-4xl`から`max-w-[1600px]`に変更し、1920px幅ではテーブル内スクロール不要（1566px確保）、1440px幅でも必要幅までの差が620px→76pxまで縮小することを確認。SC02/SC07/SC08は`max-w-4xl`のままでも内部の表・グラフがコンテナ幅に収まりテーブル内スクロールも発生しないため変更なし（SC02/SC08は表がなくカード主体、SC07のクロス集計テーブルは列数が固定〔費目名＋12ヶ月＋年間合計〕でコンテナ幅862pxに収まる）。`tsc --noEmit`通過確認済み
  - [x] 予算超過アラート（2026-08-15）。`frontend/src/components/WeeklyBudgetProgress.tsx`を拡張し、費目ごとの「超過」バッジと見出し横の「n件超過」件数バッジを追加。バックエンド変更なし（既存の週次予算実績データで判定）
  - [x] 定期取引（2026-08-15）。`recurring_transactions`テーブル（費目・対象者・金額・メモ・毎月の日付1〜28・有効フラグ）を追加し、`transactions`に生成元を辿る`recurring_transaction_id`（nullable）を追加（マイグレーション`20260815150000_add_recurring_transactions_and_savings_goals`）。`backend/src/services/recurringTransactionService.ts`でCRUD（`categoryId`は固定費/変動費のみ許可）と`generateDueTransactions(householdId, year, month, triggeredByUserId)`（当月分の定期取引がまだ生成されていなければ生成、`recurringTransactionId`で二重生成を防止）を実装。cronサーバーを持たない構成のため、`GET /summary/monthly`が**実際の「今月」を表示したときだけ**（過去月を遡って生成しないよう`year/month`をサーバー日時と比較）自動生成をトリガーする設計（`routes/summary.ts`）。単体テストは`backend/tests/recurringTransactions.test.ts`（当月生成・二重生成防止・過去月は生成されないことを含む）。フロントはSC11に「定期取引」タブ（`frontend/src/components/RecurringTransactionsPanel.tsx`）を追加し登録・一覧・無効化に対応（実行操作は不要、ダッシュボード表示時に自動）
  - [x] 貯金目標（2026-08-15）。`savings_goals`テーブル（目標名・目標金額・有効フラグ）を追加。進捗計算用の実績値は新規API不要とし、既存の`GET /summary/annual`の集計結果から`MonthlySavingsSummary.tsx`が既に持つ`calculateMonthlyTotalSaving`ロジック（貯金合計=収入−支出）と同じ考え方を`SC08_Visualization.tsx`側で`VisualizationMember.annual.totalSaving`を世帯全員分合算する形で流用（`backend/src/services/savingsGoalService.ts`はCRUDのみ）。フロントはSC08に`frontend/src/components/SavingsGoalSection.tsx`を追加し、選択中年の実績に対する達成率をプログレスバー表示。単体テストは`backend/tests/savingsGoals.test.ts`
  - [x] PWA化（2026-08-15）。`vite-plugin-pwa`を導入し`frontend/vite.config.ts`にmanifest設定（アイコンはデザインアセットがないため`frontend/public/icon.svg`のシンプルな自作SVGを使用、192/512/maskableの3エントリ全て同一ファイル）。API通信（`/api/`）はservice workerでキャッシュしない設計（`runtimeCaching`未設定、ビルド成果物のみプリキャッシュ）とし、金額データの鮮度を優先。`vite build`で`sw.js`・`manifest.webmanifest`生成を確認済み
  - [x] ダークモード（2026-08-15）。`tailwind.config.js`に`darkMode: 'class'`を設定し、`frontend/src/theme.ts`・`frontend/src/hooks/useTheme.ts`でテーマの永続化（`localStorage`の`kakeibo_theme`）とOSの`prefers-color-scheme`を初期値として尊重する仕組みを実装。`main.tsx`で描画前にテーマを適用しちらつきを防止。`NavBar.tsx`にトグルボタンを追加。`frontend/src/index.css`の`body`に基本背景色を設定。残り全画面・全コンポーネント（`NavBar.tsx`・`components/charts/`配下のChart.jsコンポーネントを除く36ファイル）への`dark:`バリアント追加はサブエージェントに委譲（色の対応表を指定して一貫性を担保）。**Chart.js（`DoughnutChart`/`LineChart`/`StackedBarChart`）内部の軸・凡例・グリッド線の色はcanvas描画のためTailwindが効かず、スコープ外として現状ライト固定のまま**（テーマ状態をchart optionsに渡す仕組みが別途必要）。`tsc --noEmit`・`vite build`通過確認済み
  - [x] フロントエンドのテスト整備（2026-08-15）。`vitest`・`@testing-library/react`は導入済みだったが実テストが0件だったため、代表例として4ファイル・18テストを追加：`MonthlySavingsSummary.test.ts`（貯金合計計算の純粋関数）、`WeeklyBudgetProgress.test.tsx`（超過バッジ表示）、`IconPicker.test.tsx`（開閉・選択・自由入力の操作、`@testing-library/user-event`を新規導入）、`useYearMonthParams.test.tsx`（URLクエリパラメータとの同期、`MemoryRouter`でラップ）。`frontend/src/setupTests.ts`を新規作成し`vite.config.ts`の`test.setupFiles`に登録（`test.globals: true`にしていないため`@testing-library/react`の自動クリーンアップが効かず、`afterEach(cleanup)`を明示登録する必要があった。当初これが未実装で複数テストの描画が蓄積し`getByText`が「複数要素が見つかる」エラーになる不具合があったため修正済み）
  - [x] 割り勘・立て替え精算の自動計算（F16、2026-08-15）。「対象者=両方（shared）」「対象者=相手個人（self, userId≠createdBy）」の取引を支払者(`createdBy`)ごとに集計し、((①/2)+③)-((②/2)+④)相当の式でどちらがどちらにいくら払うべきかを自動算出する新機能。`transactions`テーブルに`otherPaidAmount`（相手がその場で直接払った金額、nullable、Decimal(10,0)）を追加し、精算計算時に各取引の貸し`due(t)=baseAmount(t)-otherPaidAmount(t)`から差し引く（マイグレーション`20260815160000_add_transaction_other_paid_amount`）。`backend/src/services/summaryLogic.ts`に純粋関数`calculateSettlement`を追加：端数は個々の取引でfloorせず最終合計にのみ`Math.sign(rawNet)*Math.round(Math.abs(rawNet))`で対称丸め（`Math.round`単体だと負数の.5が0側に丸められ支払者の入れ替えだけで金額が変わる不公平が生じるため）。単体テストは`backend/tests/summaryLogic.test.ts`。新規`backend/src/services/settlementService.ts`（household内ユーザーがちょうど2人であることのみ明示的に検証、`GET /summary/settlement?startDate=&endDate=`として`routes/summary.ts`に追加）。`transactionService.ts`の`validateInput`には`0<=otherPaidAmount<=amount`の範囲チェックのみ追加し、「splitType=self かつ userId=createdBy（完全自分用）にはotherPaidAmount禁止」という追加ルールは意図的に設けていない（精算計算では当該取引が最初から対象外になり実害がないため、`createdBy`をvalidateInputに渡す設計変更のリスクに見合わないと判断）。フロントは新規`frontend/src/pages/Settlement.tsx`（`useDateRangeParams`で`?startDate=&endDate=`をURL共有、デフォルトは当月1日〜今日）＋`DateRangeSelector`/`SettlementResultCard`/`SettlementBreakdownTable`の3コンポーネント、`NavBar`に「精算」タブとして追加。`EditTransactionModal.tsx`に「相手がその場で払った金額（任意）」の入力欄を追加（対象者が「両方」または「自分以外」の時のみ表示、3タップ設計を維持するためSC03取引入力画面には追加していない）。バックエンド148テスト・フロントエンド22テスト全通過、`tsc --noEmit`・`vite build`通過確認済み。Playwrightでdev環境に対し実際に取引登録→精算画面での計算結果表示（内訳・方向・金額）→編集画面での新フィールド操作まで動作確認済み（コンソールエラーなし）

### 補足（2026-08-13時点）
- 開発用MySQLコンテナ（`kakeibo-mysql`）に、現行設計と不整合な旧`categories`/`transactions`テーブル（`household_id`なし、ダミーデータ16件）が残存していたため削除済み。バックアップは開発者のスクラッチ領域に保存（リポジトリ外）。今後同様の旧データが必要な場合は要相談。
- `kakeibo_user`にシャドウDB作成用の`CREATE, DROP, ALTER, REFERENCES, INDEX`グローバル権限を付与済み（`prisma migrate dev`に必要）。ただしこれらの権限だけでは不十分で、2026-08-15時点で`prisma migrate dev`実行時に`20260815120000_add_user_auth`（`UPDATE`文でユーザーへ開発用仮パスワードを一括付与するデータ移行を含む）をシャドウDB上で再生する際に`UPDATE command denied`で失敗することを確認済み（`UPDATE`等のDML権限がシャドウDBには付与されていないため）。恒久対応（権限追加）はスコープ外としたため、新規マイグレーションが必要な場合は`prisma migrate dev`ではなく、マイグレーションフォルダ・`migration.sql`を手動作成した上で`prisma migrate deploy`（シャドウDBを使わず本番/開発DBに直接適用）→`prisma generate`の順で実行すること。
- `users`テーブルは認証機能未実装のため`households`と同様の最小限スタブだったが、2026-08-13にSC05表示用の`display_name`・`household_id`を追加済み。2026-08-15にJWT認証実装に伴い`email`/`password_hash`/`color_code`も追加済み（詳細はフェーズ0参照）。
- 開発用DBのhousehold_id=1に、SC05動作確認用のダミーデータ（ユーザー2名「たいよう」「みらの」、収入費目「給与」、先取り貯金費目「積立NISA」）を投入済み。本番投入データではないため、必要に応じて削除・差し替えて問題ない。2026-08-15のJWT認証実装で両ユーザーに開発用ログイン情報を設定済み（`taiyo@example.com`/`mirano@example.com`、パスワードは共通で`kakeibo1234`）。